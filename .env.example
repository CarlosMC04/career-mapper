# Copy this file to .env and set real values in your deployment provider
# Render: use the Dashboard -> Environment -> Add Variables
# Railway/Fly/etc: set as service variables/secrets

# JWT secret used to sign auth tokens
JWT_SECRET=change_this_in_production

# Optional. Most hosts inject PORT automatically.
PORT=3000
